class Solution {
    public int missingNumber(int[] nums) {
        int size = nums.length;
        int n = 0;
        Arrays.sort(nums);
        for(int i = 0; i <= nums.length-1; i++){
            if(nums[i]==n){
                n++;
            }

        }
        return n;
        
    }
}
